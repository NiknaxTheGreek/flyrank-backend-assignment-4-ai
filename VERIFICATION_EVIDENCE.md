# Assignment 4 Verification Evidence

## Current status

The strongest current reproducible checkpoint is **GitHub Actions run 32713258765** on 2026-08-24. It verifies the repaired S3 contract from a clean checkout.

| Area | Current evidence | Status |
| --- | --- | --- |
| Clean installation | `python -m pip install .` | PASS |
| Automated contract suite | **26 tests passed** | PASS |
| Curl lifecycle | signup/login/public/protected/invalid-token/logout/token-reuse checks | PASS |
| Swagger UI | Bearer authorization + `/protected/profile` Try it out returned `200` | PASS |
| Swagger screenshot | `swagger-auth.png` captured in Actions artifact `assignment-4-auth-evidence` | PASS |
| Real provider path | preserved local Supabase CLI Auth runtime evidence | PASS |

Current acceptance markers:

```text
A4_CURL_AUTH_FLOW=PASS
A4_LOGOUT_204_AND_REUSE_401=PASS
A4_SWAGGER_BEARER_TRY_IT_OUT=PASS
```

GitHub Actions artifact:

- name: `assignment-4-auth-evidence`
- artifact ID: `9514918717`
- generated by run: `32713258765`
- contents include curl headers/bodies, local server logs, and `swagger-auth.png`

The screenshot was inspected after generation. It shows the Assignment 4 Swagger UI, lock icons on protected endpoints, `/protected/profile` expanded after authorization, Swagger's generated curl command containing a Bearer header, and server response code `200` with safe user metadata.

## Current curl checkpoint

The current CI starts the real FastAPI application with only the AuthProvider dependency overridden by a deterministic evidence provider. It then drives the contract with command-line curl and records the headers/bodies.

Observed/asserted behavior:

- `GET /public/info` → `200` without credentials;
- `POST /auth/signup` → `201`;
- `POST /auth/login` → `200` and access token;
- `GET /protected/profile` with valid Bearer token → `200`;
- `GET /protected/dashboard` with the same token → `200`;
- invalid Bearer token → `401`;
- `POST /auth/logout` → `204` with an empty response body;
- reuse of the logged-out token → `401`.

This acceptance harness is intentionally deterministic and secret-free. It proves the HTTP/OpenAPI contract and reusable dependency behavior, not a hosted Supabase account.

## Preserved genuine local Supabase Auth evidence

A separate earlier runtime checkpoint used the official Supabase CLI with a temporary local project containing Postgres, GoTrue/Auth and Kong. No hosted account or committed credential was used.

Direct Supabase Auth behavior recorded at that checkpoint:

| Request | Result |
| --- | --- |
| `GET /auth/v1/settings` | `200` |
| `POST /auth/v1/signup` | `200` |
| `POST /auth/v1/token?grant_type=password` | `200` |
| `GET /auth/v1/user` with returned Bearer token | `200`; same identity |
| `POST /auth/v1/logout` | `204` |
| Auth settings after logout | `200`; provider remained reachable |

That checkpoint also exercised the FastAPI application against the local Supabase service and recorded successful signup/login/protected identity access plus expected `401`/`403` behavior. Its old application-level logout observation was captured before the current S3 repair and therefore is **not** used as evidence for today's required logout status; the current Actions gate is the authoritative `204` proof.

## Automated test evidence

Current GitHub Actions result:

```text
26 passed, 1 non-failing warning
```

The warning is from the FastAPI/Starlette TestClient integration and does not change the test result.

Tests cover the required route/status/security contract, including missing request fields, invalid credentials, provider outage handling, public access, missing/malformed Bearer headers, invalid/expired/tampered tokens, reusable protected-route access, safe metadata, logout, token reuse, authorization and OpenAPI security declarations.

## Secret and provenance statement

No hosted Supabase verification is claimed. Real credentials are not included in this evidence package. The evidence-only provider exists solely to make curl and browser acceptance reproducible without a secret; production token verification still uses the Supabase provider. The AI-generated repository remains separate from the human-created Assignment 4 implementation.

## AI Rematch boundary

The later S4 human-vs-AI Rematch comparison is project-required once the human-led implementation is complete and understood. It is not fabricated or marked complete here.
